# ===========================================
# 🚀 DEPLOYMENT ENVIRONMENT CONFIGURATION
# ===========================================
# Copy this file to .env and fill in your values
# Never commit .env files to version control!
#
# This project deploys to:
#   • Backend  → Render (Web Service)
#   • Frontend → Vercel
#   • Database → Turso (libsql)

# ===========================================
# DATABASE - TURSO (Production)
# ===========================================
# Get these from: https://turso.tech/app
# 1. Create a database in Turso dashboard
# 2. Copy the database URL and auth token

TURSO_DATABASE_URL=libsql://your-db-name.turso.io
TURSO_AUTH_TOKEN=your_auth_token_here

# ===========================================
# BACKEND - RENDER
# ===========================================
# Set these in Render dashboard → Environment

# Server port (Render uses PORT env variable automatically)
PORT=8080

# Environment mode: development | production
NODE_ENV=development

# JWT Secret - IMPORTANT: Use a strong random string in production!
# Generate one: openssl rand -base64 32
JWT_SECRET=your_jwt_secret_here_use_strong_random_string

# Allowed origins for CORS (comma-separated for multiple)
# In production, set this to your Vercel frontend URL
ALLOWED_ORIGINS=http://localhost:5173,https://your-app.vercel.app

# ===========================================
# FRONTEND - VERCEL
# ===========================================
# Set these in Vercel dashboard → Settings → Environment Variables
# For Vite: All variables must be prefixed with VITE_

# API Base URL - Your Render backend URL
# Development: http://localhost:8080
# Production:  https://your-app-name.onrender.com
VITE_API_BASE=http://localhost:8080

# ===========================================
# OPTIONAL - RSS Article Sources
# ===========================================
# Comma-separated list of RSS feed URLs for robotics articles
ROBOTICS_ARTICLE_SOURCES=
ROBOTICS_ARTICLE_LIMIT=5
